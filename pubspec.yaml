name: foliox
description: "A new Flutter project." # Keep only one description key
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.3.4
  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.1
  http: ^1.2.2
  html: ^0.15.0
  xml: ^6.1.0
  url_launcher: ^6.1.5
  cloud_firestore: ^5.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Adding assets section
  assets:
    - assets/retire.png
    - assets/emergency.png
    - assets/home.png
    - assets/marriage.png
    - assets/business.png
    - assets/car.png
    - assets/invest.png
    
